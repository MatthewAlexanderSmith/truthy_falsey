<h1>New Fact</h1>
<%= form_for :fact do |f| %>
  <div class="field">
    <%= f.label :body %><br/>
    <%= f.text_area :body, autofocus: true %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>